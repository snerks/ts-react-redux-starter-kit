import * as React from 'react';
export class Badge extends React.Component {
    render() {
        return (React.createElement("span", {"className": 'container text-center'}));
    }
}
export default Badge;
